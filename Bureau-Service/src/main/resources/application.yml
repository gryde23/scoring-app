server:
  port: 8083

spring:
  application:
    name: Bureau-Service
  datasource:
    url: jdbc:postgresql://localhost:5433/bureau_db
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
  liquibase:
    enabled: true